import styled from 'styled-components'

function Calendar({ setFilter }) {
	return (
		<S.Wrapper>
			<div></div>
		</S.Wrapper>
	)
}
export default Calendar

const Wrapper = styled.div``

const S = { Wrapper }
