React_query
비동기 상태 관리 라이브러리



isLoading, error, data

useQuery(queryKey, queryFn?,{
    
})

query 고유한 키를 제공하고 키이름으로 메모리를 보관한다.
두번째 컴포넌트가 동일한 키를 사용하면 캐싱한 데이터를 사용하므로 따로 데이터를 호출하지않는다.

캐싱한 데이터를 사용하는지 아닌지 확인해야한다.

useQuery useInfiniteQuery를 사용하면 기본적으로 캐싱데이터는 stale상태로 처리한다